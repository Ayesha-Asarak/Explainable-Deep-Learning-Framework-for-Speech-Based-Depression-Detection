# 01 — Extra training scripts

Not the deployed path. Includes CNN (`train.py`), SSL, LoRA, multimodal, etc.

Deployed training stays at repo root: `train_official_acoustic.py`.

Run from project root so `src` imports work:
```bash
cd /path/to/Data
python3 experiments/01_Extra_Training_Scripts/train.py
```
